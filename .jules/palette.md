## 2026-06-08 - ARIA Labels for Icon-only components
**Learning:** Icon-only buttons wrapped in `TouchableOpacity` require both `accessibilityRole="button"` and `accessibilityLabel` for proper screen reader accessibility in React Native.
**Action:** Add these attributes to all interactive icon elements.
