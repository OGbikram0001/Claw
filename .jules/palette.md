## 2024-05-23 - Added ARIA labels to BottomBar icon-only buttons
**Learning:** Icon-only custom components using `TouchableOpacity` require explicit `accessibilityRole="button"` and `accessibilityLabel` for screen readers.
**Action:** Always add these props to custom icon buttons.
