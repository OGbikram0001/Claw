# MongoDB Connection Settings
MONGO_URL=mongodb://localhost:27017
DB_NAME=testdb

# CORS Configuration
# Comma-separated list of trusted origins (e.g., http://localhost:3000,https://example.com)
# Defaults to empty list (rejecting all cross-origin requests) if not set.
ALLOWED_ORIGINS=http://localhost:3000
